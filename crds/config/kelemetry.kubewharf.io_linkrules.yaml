---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: linkrules.kelemetry.kubewharf.io
spec:
  group: kelemetry.kubewharf.io
  names:
    kind: LinkRule
    listKind: LinkRuleList
    plural: linkrules
    singular: linkrule
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LinkRule defines a parent-child relation between objects in a
          Kelemetry trace.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          filter:
            description: Filter determines whether an object is a child object that
              matches this rule.
            properties:
              resources:
                description: Resources are the possible resource types that a child
                  object may belong to.
                items:
                  description: GroupResource specifies a Group and a Resource, but
                    does not force a version.  This is useful for identifying concepts
                    during lookup stages without having partially valid types
                  properties:
                    group:
                      type: string
                    resource:
                      type: string
                  required:
                  - group
                  - resource
                  type: object
                type: array
              selector:
                description: Selector selects matching child objects by label.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: A label selector requirement is a selector that
                        contains values, a key, and an operator that relates the key
                        and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: operator represents a key's relationship to
                            a set of values. Valid operators are In, NotIn, Exists
                            and DoesNotExist.
                          type: string
                        values:
                          description: values is an array of string values. If the
                            operator is In or NotIn, the values array must be non-empty.
                            If the operator is Exists or DoesNotExist, the values
                            array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: matchLabels is a map of {key,value} pairs. A single
                      {key,value} in the matchLabels map is equivalent to an element
                      of matchExpressions, whose key field is "key", the operator
                      is "In", and the values array contains only "value". The requirements
                      are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - resources
            - selector
            type: object
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          parent:
            description: Parent indicates how to find the parent object from a matched
              child object.
            properties:
              clusterTemplate:
                description: "ClusterTemplate is a Go text template string to compute
                  the cluster name of the parent object. \n The context of the template
                  is the child object that matched the rule."
                type: string
              group:
                type: string
              nameTemplate:
                description: "NameTemplate is a Go text template string to compute
                  the parent object name. \n The context of the template is the child
                  object that matched the rule."
                type: string
              resource:
                type: string
              scope:
                description: "Scope specifies whether the parent object is namespaced
                  or cluster-scoped. \n If scope=Namespaced, the parent object namespace
                  is inferred from the child object namespace. Behavior is unspecified
                  the child is cluster-scoped but the parent is namespace-scoped."
                type: string
              version:
                type: string
            required:
            - clusterTemplate
            - group
            - nameTemplate
            - resource
            - scope
            - version
            type: object
        required:
        - filter
        - parent
        type: object
    served: true
    storage: true
